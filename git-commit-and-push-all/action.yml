name: 'Add all, commit and push changes'

inputs:
  message:
    description: 'The commit message'
    required: true

runs:
  using: "composite"
  steps:
    - run: git commit -a -m "${{ inputs.message }}"
      shell: bash
    - run: git push origin
      shell: bash
